import 'package:flutter/material.dart';

abstract interface class AppColors {
  static const Color seedColor = Color(0x000064ff);
  Color get surfaceColor;

  Color get imageBoxBackgroundColor;
  // implement corresponding colors if necessary
  // Color get imageBoxIconColor;
  // Color get imageBoxTextColor;
}
